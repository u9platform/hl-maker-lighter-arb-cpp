# Baseline 2026-03-27

Environment:

- command: `./native_baseline 5`
- host: local development machine

Native public-market-data sample:

- `native_hl_orderbook`
  - mean: `57.76ms`
  - max: `125.78ms`
- `native_lighter_orderbook`
  - mean: `32.75ms`
  - max: `33.90ms`

Updated interpretation:

- both venues are now on the same order of magnitude in the pure C++ polling path
- the old Python bridge bottleneck is gone from the runtime hot path
- the next meaningful latency reduction will come from websocket market data and live trading benchmarks
